import { DefaultSidebarProps } from '../types/sidebar';

/* Setting the default value of the isOpen property to false. */
const defaultValue: DefaultSidebarProps = {
  isOpen: false
};

export { defaultValue };
